CREATE DATABASE wireable
CREATE TABLE BackEndProject(
	id INT NOT NULL PRIMARY KEY,
	username nvarchar(50) NOT NULL,
	password nvarchar(50) NOT NULL,
	userid INT NOT NULL PRIMARY KEY,
	primary key(id),
	)

CREATE TABLE BackEndProject(
	id INT NOT NULL PRIMARY KEY,
	username nvarchar(50) NOT NULL,
	password nvarchar(50) NOT NULL,
	userid INT NOT NULL PRIMARY KEY,
	primary key(id),
	)

BEGIN

RETURN username
RETURN password
RETURN userid

END